# Input data

`site_sample.json` is a ROOT RDF sample spec for the local FlashSim NanoAOD file. The ROOT file itself is external to this repository. To use another sample, copy the spec, change its `trees` and `files` entries, and pass `--spec your_spec.json` to the Python examples and studies.
